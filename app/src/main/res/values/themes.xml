<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App theme shown after the system splash hands off. -->
    <style name="Theme.Boomeranger" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/ink</item>
        <item name="android:windowBackground">@color/ink</item>
    </style>

    <!-- Android 12+ system splash → then Theme.Boomeranger. -->
    <style name="Theme.Boomeranger.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.Boomeranger</item>
    </style>
</resources>
